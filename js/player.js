export class Player {
    id;
    firstName;
    lastName;
    ranking;
    money;

    constructor(id, firstName, lastName) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}